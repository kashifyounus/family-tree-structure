name: Mobile — Maestro smoke

on:
  pull_request:
    paths:
      - "family-tree-app/**"
      - ".github/workflows/mobile-maestro.yml"
  push:
    branches: [main]
    paths:
      - "family-tree-app/**"
  workflow_dispatch:

concurrency:
  group: mobile-maestro-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  maestro-contracts:
    name: Maestro flow contracts
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: family-tree-app
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: family-tree-app/package-lock.json

      - name: Install and validate flows
        run: |
          npm ci
          npm run test:maestro:contracts

  maestro-onboarding-smoke:
    name: Android emulator — onboarding flow
    needs: maestro-contracts
    # Emulator E2E is slow/flaky on hosted runners; run on main and manual dispatch.
    if: github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && github.ref == 'refs/heads/main')
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: family-tree-app/package-lock.json

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Set up Android SDK (build)
        run: |
          set -euxo pipefail
          SDK_ROOT="$HOME/android-sdk"
          mkdir -p "$SDK_ROOT/cmdline-tools"
          curl -fsSL -o /tmp/cmdline-tools.zip \
            https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip
          unzip -q /tmp/cmdline-tools.zip -d "$SDK_ROOT/cmdline-tools"
          mv "$SDK_ROOT/cmdline-tools/cmdline-tools" "$SDK_ROOT/cmdline-tools/latest"
          export ANDROID_HOME="$SDK_ROOT"
          export ANDROID_SDK_ROOT="$SDK_ROOT"
          export PATH="$PATH:$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools"
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager \
            "platform-tools" \
            "platforms;android-35" \
            "build-tools;35.0.0" \
            "ndk;27.1.12297006"
          echo "ANDROID_HOME=$ANDROID_HOME" >> "$GITHUB_ENV"
          echo "ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT" >> "$GITHUB_ENV"

      - name: Install dependencies and build debug APK
        working-directory: family-tree-app
        run: |
          npm ci
          npx expo prebuild --platform android --no-install
          cd android
          ./gradlew assembleDebug --no-daemon

      - name: Maestro onboarding smoke (API 30 emulator)
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 30
          arch: x86_64
          target: google_apis
          profile: pixel_6
          disable-animations: false
          emulator-boot-timeout-in-minutes: 20
          cores: 2
          ram-size: 4096M
          emulator-options: -no-snapshot-save -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim
          script: |
            chmod +x family-tree-app/scripts/ci-maestro-smoke.sh
            export ANDROID_HOME="$ANDROID_SDK_ROOT"
            export MAESTRO_CI_FLOWS="maestro/flows/01-onboarding-private-archive.yaml,maestro/flows/06-link-existing-spouse.yaml,maestro/flows/07-link-existing-child.yaml"
            family-tree-app/scripts/ci-maestro-smoke.sh

      - name: Upload Maestro debug output
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: maestro-smoke-debug
          path: |
            ~/.maestro/tests/**
          retention-days: 5
          if-no-files-found: ignore
