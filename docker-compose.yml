services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: kinship
      POSTGRES_PASSWORD: kinship
      POSTGRES_DB: kinship
    ports:
      - "5432:5432"
    volumes:
      - kinship_pg:/var/lib/postgresql/data

volumes:
  kinship_pg:
